FROM node:20-alpine

WORKDIR /app

COPY package*.json .
RUN npm ci

# The source is mounted as a volume at runtime for hot reload;
# COPY here only ensures the image can build standalone if needed.
COPY . .

EXPOSE 5173

CMD ["npm", "run", "dev", "--", "--host"]
