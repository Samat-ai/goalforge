# Node
node_modules/
dist/

# Python
.venv/
__pycache__/
*.pyc

# Secrets
.env
.env.*
apps/api/secrets/

# IDEs and Tools
.idea/
.claude/
.playwright-mcp
.worktrees

# Planning docs (local only)
docs/superpowers/
.superpowers/

# Design handoff reference (local-only prototype source for the 1:1 redesign)
design_handoff_goalforge/

# Local-only compose override (republishes db:5432 for local alembic/psql —
# committing it would re-expose Postgres on the droplet, undoing PR #180)
docker-compose.override.yml
.wrangler/

# design-sync (claude.ai/design) — staged scripts + build output + machine state;
# .design-sync/config.json, NOTES.md, conventions.md, previews/, overrides/ ARE committed
.ds-sync/
ds-bundle/
.design-sync/.cache/
.design-sync/learnings/
.design-sync/node_modules
